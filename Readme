# Streaming images over a network using python_socket connection

# Requirements
* client(image capturing device) and server(device to save the images) should be in the same network
* python, python-socket, opencv, numpy
* client is RPI and the RPI camera has to be used for capturing images.


